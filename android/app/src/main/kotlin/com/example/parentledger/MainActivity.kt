package com.example.parentledger

import io.flutter.embedding.android.FlutterActivity

// Legacy launcher target retained for builds or devices that still reference
// the original Flutter package name after the applicationId rename.
class MainActivity : FlutterActivity()
